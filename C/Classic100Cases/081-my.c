/*
题目：809*??=800*??+9*?? 其中??代表的两位数, 809*??为四位数，8*??的结果为两位数，9*??的结果为3位数。求??代表的两位数，及809*??后的结果。

程序分析：无。
*/

#include <stdio.h>

int main(void)
{
    for (size_t i = 10; i < 99; i++)
    {
        if (809 * i > 9999 || 8 * i > 99 || 9 * i > 999)
            break;
        if (809 * i >= 1000 && 8 * i >= 10 || 9 * i >= 100)
            if (809 * i == 800 * i + 9 * i) //这句是废话，主要题意中809*??=800*??+9*??是废话，或者有其他深意？
                printf("%d 满足题意。\n", i);
    }

    return (0);
}