# 集合
# 集合的键（值）必须时可哈希化的类型，即hash(key)不抛异常

# 1. 声明
set0 = set()
set0 = {"C", "C++", "C#", "Python", "SQL", "JavaScript", "R", "Go"}
set1 = set([1, 2, 3])           # set(列表)是集合，可以看做没有值的字典
set2 = set([1, 1, 2, 2, 3, 3])  # 集合中没有重复值，可以使用集合给列表去重复
